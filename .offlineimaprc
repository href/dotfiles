[general]
ui = basic
accounts = fastmail
maxsyncaccounts = 4
socktimeout = 60
pythonfile = ~/bin/keychain

[Account fastmail]
localrepository = fastmail-local
remoterepository = fastmail-remote

[Repository fastmail-local]
type = Maildir
localfolders = ~/Mail/Fastmail

[Repository fastmail-remote]
type = IMAP
remotehost = mail.messagingengine.com
remoteport = 993
ssl = yes
cert_fingerprint = 54137524fe721f0cbb8a6a0e1e280ade50482e29
holdconnectionopen = yes
remoteuser = href@fastmail.fm
remotepasseval = get_keychain_pass(account="href@fastmail.fm", server="mail.messagingengine.com")

expunge = no
folderfilter = lambda foldername: foldername not in ['INBOX.Trash']
